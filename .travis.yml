language: cpp

compiler:
  - gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-8
      - g++-8
      - libboost-all-dev

script:
  - cd ${TRAVIS_BUILD_DIR}
  - . ./run install
  - . ./run port
